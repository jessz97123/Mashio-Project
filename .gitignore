# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude Code dev config (machine-specific paths)
.claude/

# Raw source assets — NOT used by the build (the live site uses the optimized
# copies in public/). Kept locally only, to keep the repo small & fast to deploy.
/Photo/
/Drinks PNG/
*.ai
